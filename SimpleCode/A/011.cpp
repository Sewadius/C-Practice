// Логические операции
#include <iostream>

int main() {
    std::cout << (4 > 5) << std::endl;  // 0
    std::cout << (4 < 5) << "\n";       // 1
    std::cout << (5 == 5) << "\n";      // 1
    std::cout << (5 != 5) << "\n";      // 0
    return 0;
}
