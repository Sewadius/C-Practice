#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
    // Здесь продолжайте программу
    srand(time(NULL));

    int
        r1 = rand(), 
        r2 = rand(),
        r3 = rand(),
        r4 = rand(),
        r5 = rand();

    // __ASSERT_TESTS__
    return 0;
}
