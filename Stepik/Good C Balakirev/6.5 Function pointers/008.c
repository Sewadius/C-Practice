#include <stdio.h>

int sum_ar(const int *ar, size_t len_ar /* здесь указатель на функцию */)
{
    // здесь продолжайте функцию
}

int is_even(int x)
{
    return x % 2 == 0;
}

int main(void)
{
    int marks[20] = {0};
    int x;
    while (scanf("%d", &x) == 1) {
        // здесь продолжайте программу
    }

    return 0;
}
