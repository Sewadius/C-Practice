#include <stdio.h>

int main(void)
{
    float t1, t2, t3, t4, t5;
    scanf("%*f; %*f; %*f; %*f; %*f; %f; %f; %f; %f; %f;", &t1, &t2, &t3, &t4, &t5);
    printf("%.1f %.1f %.1f %.1f %.1f", t1, t2, t3, t4, t5);
    return 0;
}
